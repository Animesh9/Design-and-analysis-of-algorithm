#include<iostream>
using namespace std:
int main() 
{
    int a,b,rows;
   cout<<"Enter number of rows: ";
    cin>>rows;
    for (a=1; a<=rows; ++a) {
        for (a=1; b<=a; ++b)
        { 
          cout<<b
        }
        cout<<\n;
    }
    return 0;
}
